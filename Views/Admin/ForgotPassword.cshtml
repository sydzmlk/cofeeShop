@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Şifrəni Unutdum</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />

    <style>
        body {
            height: 100vh;
            background: linear-gradient(135deg,#0f2027,#203a43,#2c5364);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI';
        }

        .forgot-card {
            width: 450px;
            padding: 40px;
            border-radius: 12px;
            background: white;
            box-shadow: 0 10px 35px rgba(0,0,0,0.25);
        }

        .logo {
            text-align: center;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 10px;
            color: #2c5364;
        }

        .title {
            text-align: center;
            font-weight: 600;
            margin-bottom: 25px;
        }

        .form-control {
            height: 45px;
        }

        .btn-send {
            height: 45px;
            font-weight: 600;
        }

        .links {
            text-align: center;
            margin-top: 15px;
        }
    </style>
</head>

<body>

    <div class="forgot-card">

        <div class="logo">
            ☕ CoffeeShop
        </div>

        <h4 class="title">Şifrəni Unutdum</h4>

        <form method="post">
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label class="form-label">Username</label>
                <input name="username" class="form-control" placeholder="Username daxil edin" required />
            </div>

            <button class="btn btn-primary w-100 btn-send">Kod göndər</button>

            @if (ViewBag.Message != null)
            {
                <div class="alert alert-info mt-3 text-center">
                    @ViewBag.Message
                </div>
            }

            <div class="links">
                <a href="/Admin/Login">Login səhifəsinə qayıt</a>
            </div>

        </form>

    </div>

</body>
</html>